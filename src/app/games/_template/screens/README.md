# Screens

Maak per scherm een eigen map.

Voorbeeld:

```txt
screens/
  start/
    StartScreen.tsx
    StartHeader.tsx
    StartActions.tsx
```

Een screen mag alleen layout en wiring doen. Logica hoort in `state/` of `logic/`.

