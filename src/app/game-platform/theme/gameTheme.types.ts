export type GameThemeTone = "danger" | "neutral" | "primary" | "reward" | "secondary";

export interface GameTheme {
  danger: string;
  neutral: string;
  panel: string;
  primary: string;
  reward: string;
  secondary: string;
}

